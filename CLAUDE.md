# おかスノ LINE Bot — Claude Code 行動規約

## プロジェクト概要

スノーボードグッズレンタルショップ「おかスノ」のLINEカスタマーサポートBot。

- **言語**: Python 3.9+
- **フレームワーク**: Flask
- **外部API**: LINE Messaging API v3（`line-bot-sdk`）、Google Gemini API（`google-genai`）
- **構成**: `app.py`（Webhookハンドラ）/ `gemini_client.py`（API呼び出し）/ `config.py`（設定・システムプロンプト）

## 開発者について

このプロジェクトのオーナーは化学専攻の大学4年生で、プログラミング未経験からエンジニアを目指している。このBotは最初のポートフォリオ作品であり、技術を習得しながら開発を進めるフェーズにある。

---

## 1. トークン消費の最小化

- 挨拶、謝罪、前置き、要約の繰り返しなど、タスク遂行に不要な出力を一切行わない。
- コード修正時はファイル全体を出力しない。変更が必要な関数・ブロックのみを提示する。
- 説明は要点のみ。冗長な言い換えや同じ内容の反復を避ける。

## 2. 出力精度の最大化

- このプロジェクトはPython + Flask + LINE Messaging API v3 + Anthropic SDKで構成されている。この技術スタックを前提としてコードを書くこと。
- **推測によるコーディングを禁止する。** APIの仕様、ライブラリの引数、動作仕様に確証がない場合は、検索ツール（WebSearch等）を使用して公式ドキュメントを確認するか、開発者に質問すること。
- 「たぶんこうだと思う」「おそらく動くはず」という前提でコードを書かない。

## 3. 学習のサポート

- 開発者はプログラミング初学者であり、処理の流れやシステム構造を理解しながら実装を進めるフェーズにいる。単にコードを出力するだけでなく、以下のフォーマットで**「意図」**と**「手順」**を必ず補足すること。
- **【補足フォーマット】**
  - **意図**: なぜこのコード・設計・設定が必要なのか（1〜2行）
  - **手順**: データや処理がどのような順番で流れるか（箇条書きで簡潔に）
- 新しいライブラリやAPI（Flaskのルーティング、LINEのWebhook処理など）を初めて使う箇所では、初学者がつまずきやすいポイントを先回りして解説すること。
- 全体として教科書的な長文は避け、要点のみをスッキリとまとめること。

## 4. TODOの進捗管理

- **アクション（ファイル作成・編集・コマンド実行など）を1つ行うたびに、即座に `TODO.md` を確認し、該当項目を `[ ]` から `[x]` に更新する。**
- 複数のステップを一度に実施した場合も、完了したものをまとめてチェックする。
- まだ完了していない項目は絶対にチェックしない。

## 5. モデル最適化（Opus / Sonnet / Haiku 使い分け）

このプロジェクトでは以下のハイブリッド運用を基本方針とする:

| モデル | 用途 |
|---|---|
| **Opus** | 要件定義、全体設計、複雑なバグ解決、アーキテクチャ判断 |
| **Sonnet** | 日常的な機能実装、リファクタリング、中程度のバグ修正 |
| **Haiku** | 単純な質問応答、軽微な修正、フォーマット調整、定型作業 |

**モデル切り替えの提案ルール:**
- Opusで単純なコーディング作業（変数名変更、フォーマット修正等）を依頼された場合 → タスク実行前に「このタスクはSonnetまたはHaikuで十分です。`/model` で切り替えを推奨します」と提案する。
- Sonnetで複雑なアーキテクチャ設計や全体の要件定義を求められた場合 → タスク実行前に「このタスクはOpusが適しています。`/model` で切り替えを推奨します」と提案する。
- Haikuで中程度以上の実装を求められた場合 → 適切なモデルへの切り替えを提案する。
- 提案後、開発者が現在のモデルでの続行を選択した場合は、そのまま対応する。
